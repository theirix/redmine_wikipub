# English strings go here for Rails i18n
en:
  wikipub:
    greeting: 'Customize a project to become a standalone wikipage served from a specific hostname'
    extraconf:
      label: 'Configuration'
      description: 'JSON with configuration. Example: { "entries" : [ {"hostname": "hostname-regex", "project": "project-name", "theme": "theme-name", "allowaccount": true, "analytics": "footer code" } ] }'
